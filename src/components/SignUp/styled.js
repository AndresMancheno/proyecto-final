import { styled } from '@nextui-org/react';

export const BackgroundColor = styled('div', {
  position: 'fixed',
  top: 0,
  left: 0,
  height: '100%',
  width: '100%',
  background: '$gradient',
});
