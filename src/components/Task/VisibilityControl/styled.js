import { styled } from '@nextui-org/react';

export const StyledVisilityControl = styled('form', {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  marginTop: '2rem',
});

export const StyledCheckbox = styled('input', {
  marginRight: '0.5rem',
});
